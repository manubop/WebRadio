<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="WebRadio.App"
             xmlns:local="using:WebRadio"
             RequestedThemeVariant="Default"
			 x:DataType="local:App">
             <!-- "Default" ThemeVariant follows system theme variant. "Dark" or "Light" are other available options. -->

	<Application.DataTemplates>
        <local:ViewLocator/>
    </Application.DataTemplates>
  
    <Application.Styles>
        <FluentTheme />
    </Application.Styles>

	<!--<TrayIcon.Icons>
		<TrayIcons>
			<TrayIcon Icon="/Assets/avalonia-logo.ico" ToolTipText="WebRadio">
				<TrayIcon.Menu>
					<NativeMenu>
						<NativeMenuItem Header="Play" Command="{Binding PlayCommand}" IsEnabled="True" ToggleType="None"/>
						<NativeMenuItemSeparator />
						<NativeMenuItem Header="Exit" Command="{Binding ExitCommand}" IsEnabled="True" ToggleType="None"/>
					</NativeMenu>
				</TrayIcon.Menu>
			</TrayIcon>
		</TrayIcons>
	</TrayIcon.Icons>-->

</Application>